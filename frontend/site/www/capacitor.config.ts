import type { CapacitorConfig } from '@capacitor/cli';

const config: CapacitorConfig = {
  appId: 'com.conversecommaria.app',
  appName: 'Converse com Maria',
  webDir: 'www'
};

export default config;
